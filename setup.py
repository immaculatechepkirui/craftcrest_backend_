from setuptools import setup, find_packages

setup(
    name="craftcrest_backend",
    version="0.0.0",
    description="Temporary package metadata for building wheels",
    packages=find_packages(),
)